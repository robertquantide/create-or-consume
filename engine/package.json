{
  "name": "create-or-consume-engine",
  "version": "0.1.0",
  "description": "Core tracking engine for Create or Consume",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target node",
    "start": "bun run src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "express": "^4.21.0",
    "active-win": "^9.0.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "bun-types": "^1.1.0",
    "typescript": "^5.6.0"
  },
  "trustedDependencies": [
    "active-win"
  ]
}
