// Create or Consume — Content Script
// Minimal content script for future use (e.g., page-level classification hints)
// Currently unused — domain-level classification happens via background.js

// Could be extended to:
// - Detect if user is typing/editing vs reading on mixed sites
// - Monitor scroll depth vs. keyboard activity
// - Detect specific page contexts (e.g., YouTube Studio vs YouTube watching)

(() => {
  // Placeholder for future enhancements
})();
